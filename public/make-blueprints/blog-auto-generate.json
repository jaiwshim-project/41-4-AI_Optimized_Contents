{
  "name": "AIO/GEO 블로그 콘텐츠 자동 생성",
  "description": "주제와 키워드를 입력하면 AIO/GEO 최적화된 블로그 콘텐츠를 자동 생성하는 시나리오",
  "version": 1,
  "modules": [
    {
      "id": 1,
      "module": "gateway:CustomWebHook",
      "name": "트리거: 콘텐츠 요청 수신",
      "parameters": {
        "note": "Make.com에서 Webhook 트리거를 설정하세요. 요청 본문에 topic, targetKeyword, category 필드를 포함합니다."
      }
    },
    {
      "id": 2,
      "module": "http:ActionSendData",
      "name": "AIO/GEO 콘텐츠 생성 API 호출",
      "parameters": {
        "url": "https://aio-geo-optimizer.vercel.app/api/webhook",
        "method": "POST",
        "headers": [
          { "name": "Content-Type", "value": "application/json" },
          { "name": "X-API-Key", "value": "{{YOUR_ANTHROPIC_API_KEY}}" }
        ],
        "body": {
          "action": "generate",
          "topic": "{{1.topic}}",
          "targetKeyword": "{{1.targetKeyword}}",
          "category": "{{1.category}}"
        }
      }
    },
    {
      "id": 3,
      "module": "http:ActionSendData",
      "name": "생성된 콘텐츠 분석 (AIO/GEO 점수 확인)",
      "parameters": {
        "url": "https://aio-geo-optimizer.vercel.app/api/webhook",
        "method": "POST",
        "headers": [
          { "name": "Content-Type", "value": "application/json" },
          { "name": "X-API-Key", "value": "{{YOUR_ANTHROPIC_API_KEY}}" }
        ],
        "body": {
          "action": "analyze",
          "content": "{{2.result.content}}",
          "targetKeyword": "{{1.targetKeyword}}"
        }
      }
    },
    {
      "id": 4,
      "module": "util:SetVariable",
      "name": "결과 저장",
      "parameters": {
        "variables": [
          { "name": "title", "value": "{{2.result.title}}" },
          { "name": "content", "value": "{{2.result.content}}" },
          { "name": "aioScore", "value": "{{3.result.aioScore}}" },
          { "name": "geoScore", "value": "{{3.result.geoScore}}" }
        ]
      }
    }
  ],
  "connections": [
    { "from": 1, "to": 2 },
    { "from": 2, "to": 3 },
    { "from": 3, "to": 4 }
  ],
  "setup_guide": {
    "step1": "Make.com에서 새 시나리오를 생성합니다.",
    "step2": "이 블루프린트를 Import합니다.",
    "step3": "YOUR_ANTHROPIC_API_KEY를 실제 Anthropic API 키로 교체합니다.",
    "step4": "Webhook URL을 복사하여 트리거로 사용합니다.",
    "step5": "테스트 요청을 보내 정상 동작을 확인합니다."
  }
}
